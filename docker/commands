docker pull postgres:17

docker run --name rosario-postgres -e POSTGRES_USER=rosario -e POSTGRES_PASSWORD=rosario123456789 -e POSTGRES_DB=azure_rosario -p 5432:5432 -d postgres:17


npx prisma generate

Cliente (A) → HTTP → Gateway → Kafka → ReportConsumer → Temporal (Cloud) → Worker (C)